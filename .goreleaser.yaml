project_name: torrent-edit-webseed
builds:
  - env: [CGO_ENABLED=0]
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

checksums:
  name_template: "checksums.txt"
